spring:
  jpa:
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never
  mvc:
    hiddenmethod:
      filter:
        enabled: true

forex:
  api:
    key: test
    url: "https://example.com"
    base: "USD"

exercises:
  api:
    baseUrl: "http://localhost:8081"

